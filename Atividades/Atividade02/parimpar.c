#include <stdio.h>

//Escrever um programa em C que determine se um número é par ou impar.

#include <stdio.h>

int main() {
    int num = 3;
    

    printf("%s\n", num % 2 == 0 ? "é par" : "é impar"); 
    

    return 0;
}
